meta {
  name: Add Category - Name Too Long
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/categories
  body: json
  auth: none
}

body:json {
  {
    "name": "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
    "description": "Valid description"
  }
}

assert {
  res.status: eq 400
}

tests {
  test("returns 400 when name exceeds 64 characters", function() {
    expect(res.status).to.equal(400);
  });
  test("returns problem details title", function() {
    expect(res.body.title).to.equal("Invalid request");
  });
}
